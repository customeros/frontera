query TestThreadQuery($threadId: String!) {
          getThreadMetadata(threadId: $threadId) {
            id
            summary
            participants
            hasAttachments
          }
        }
