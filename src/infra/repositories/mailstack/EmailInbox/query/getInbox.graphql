query getInboxQuery($userId: String!) {
  getThreadsByUser(userId: $userId) {
    id
    isDone
    isViewed
    lastMessageAt
    lastSender
    lastSenderDomain
    mailboxId
    subject
    summary
    userId
  }
}
