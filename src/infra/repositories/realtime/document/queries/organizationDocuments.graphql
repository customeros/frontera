query organizationDocuments($organizationId: ID!) {
  organizationDocuments(organizationId: $organizationId) {
    id
    name
    tenant
    userId
    icon
    color
    organizationId
    insertedAt
    updatedAt
  }
}
